# Production settings, add anything you need, or override default settings from common
# This file should not exist on your development

from common import *

DEBUG = False
TEMPLATE_DEBUG = DEBUG

# Secret key
SECRET_KEY = 'production_secret_key'


DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'dbname',
        'USER': 'dbuser',
        'PASSWORD': 'dbpassword',
        'HOST': '',                      
        'PORT': '',
    }
}